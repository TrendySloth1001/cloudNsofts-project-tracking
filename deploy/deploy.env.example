# Env for compose.prod.yml (the data services). Copy to deploy/deploy.env and
# set strong secrets. These must match the DATABASE_URL / S3_* in backend/.env.
POSTGRES_USER=cloudnsofts
POSTGRES_PASSWORD=CHANGE_ME_strong_db_password
POSTGRES_DB=cloudnsofts
MINIO_ROOT_USER=cloudnsofts
MINIO_ROOT_PASSWORD=CHANGE_ME_strong_minio_password
